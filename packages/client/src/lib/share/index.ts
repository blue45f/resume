// 공유 공통 모듈 배럴 — 원본 desk-platform/apps/web/src/lib/share/ 를 벤더링했다.
// 디자인/토스트 의존성이 없는 순수 모듈이라 호출부(페이지)에서 결과를 처리한다.
export { shareOrCopy } from './shareOrCopy'
export type { ShareInput, ShareResult } from './shareOrCopy'
export { shareResultMessage } from './shareResultMessage'
